spring.profiles.active: development
---
spring:
  profiles: development
  data:
    mongodb:
      host: ${MONGODB_SERVICE_HOST}
      port: ${MONGODB_SERVICE_PORT}
      username: test
      password: test
      db: orders
  mvc:
    date-format: "YYYY-MM-dd"
  jackson:
    date-format: "YYYY-MM-dd"
    serialization:
      write_dates_as_timestamps: true
  rabbitmq:
    host: 192.168.99.100
    port: 5672  	  
security:
  oauth2:
    resource:
      userInfoUri: http://localhost:8181/uaa/user
    client:
      client-id: acme
      access-token-uri: http://localhost:8787
  ignored: /api/**
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/  
---
spring:
  profiles: docker
  data:
    mongodb:
      host: mongo
      port: 27017
      username: test
      password: test
      db: orders	  
  mvc:
    date-format: "YYYY-MM-dd"
  jackson:
    date-format: "YYYY-MM-dd"
    serialization:
      write_dates_as_timestamps: true
security:
  oauth2:
    resource:
      userInfoUri: http://${DOCKER_IP:192.168.99.100}:8181/uaa/user
    client:
      client-id: acme
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery-service:8761/eureka/
---
spring:
  profiles: cloud
  data:
    mongodb:
#      host: ${MONGODB_SERVICE_HOST}
#      port: ${MONGODB_SERVICE_PORT}
      host: mongodb.openshift-infra
      port: 27017
      username: test
      password: test
      db: orders
  mvc:
    date-format: "YYYY-MM-dd"
  jackson:
    date-format: "YYYY-MM-dd"
    serialization:
      write_dates_as_timestamps: true
security:
  oauth2:
    resource:
#userInfoUri: http://${USER_SERVICE_SERVICE_HOST}:${USER_SERVICE_SERVICE_PORT}/uaa/user
      userInfoUri: http://user-service/uaa/user
    client:
      client-id: acme    
#access-token-uri: http://${ORDER_SERVICE_SERVICE_HOST}:${ORDER_SERVICE_SERVICE_PORT}
      access-token-uri: http://order-service
eureka:
  instance:
    prefer-ip-address: true
#non-secure-port: 8181
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
    serviceUrl:
     defaultZone: http://discovery-service-peer1/eureka/,http://discovery-service-peer2/eureka/
#http://${DISCOVERY_SERVICE_PEER1_SERVICE_HOST}:${DISCOVERY_SERVICE_PEER1_SERVICE_PORT}/eureka/,http://${DISCOVERY_SERVICE_PEER2_SERVICE_HOST}:${DISCOVERY_SERVICE_PEER2_SERVICE_PORT}/eureka/